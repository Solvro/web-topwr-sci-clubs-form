import 'package:flutter/material.dart';

class WrongUrlPage extends StatelessWidget {
  const WrongUrlPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Inproper url");
  }
}
